services:
  davis-wx:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - davis-data:/app/data
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0
    environment:
      - KANFEI_SERIAL_PORT=/dev/ttyUSB0
      - KANFEI_BAUD_RATE=19200
      - KANFEI_DB_PATH=/app/data/weather.db
    restart: unless-stopped

volumes:
  davis-data:
