# Review: PR 343 public-mode read tightening

Date: 2026-08-15
Reviewed: PR #343 diff from `56afef5` to `49322f8`
Round: 2
Label applied: approved-by-codex-agent

## What Is Correct

The round-1 blocker is fixed. `_DEFAULTS` defines `backup_schedule_time` at `backend/app/api/config.py:182`, and `_PUBLIC_MODE_HIDDEN_KEYS` now hides that same key at `backend/app/api/config.py:279`. The old misspelled `backup_schedule` entry is gone, so a public-mode `GET /api/config` no longer returns the configured backup schedule time through that typo.

The strengthened regression test catches the exact class of bug from round 1. `tests/backend/test_public_mode_read_tightening.py:183` iterates every hidden key, `tests/backend/test_public_mode_read_tightening.py:184` asserts each one exists in `_DEFAULTS`, and `tests/backend/test_public_mode_read_tightening.py:189` seeds each hidden key with a key-specific sentinel before the public-mode request. A future misspelling in `_PUBLIC_MODE_HIDDEN_KEYS` fails before response inspection because the misspelled key is not in `_DEFAULTS`.

The sentinel leak check at `tests/backend/test_public_mode_read_tightening.py:203` also covers the value-remapping case: if a hidden key's seeded value reappears under any returned key, the test fails even when the original hidden key name is absent.

I mechanically checked `_PUBLIC_MODE_HIDDEN_KEYS - _DEFAULTS` at `49322f8`; the result is empty. All 17 hidden keys correspond to real defaults.

Within the round-2 scope, I did not find a residual guest-readable ops metadata or third-party-ID leak in `GET /api/config` from the enumerated hidden class. The remaining public-mode-visible sensitive credentials are still masked by `_SECRET_KEYS`; the hidden third-party IDs and backup/last-error ops fields are filtered before response construction at `backend/app/api/config.py:307`.

## Blockers

None.

## What Needs Attention

None for the `56afef5..49322f8` delta.

## Bloat / Non-Functional

None. The added assertions are directly targeted at the bug class that escaped round 1.

## Recommendations

Keep the seeded hidden-key regression test in the suite; it is a useful guard for future edits to the hidden-key set.

## Bottom Line

Approved. The `backup_schedule_time` leak is closed, the test would fail on another nonexistent hidden-key typo, and the hidden-key set now matches `_DEFAULTS` exactly.

## Verification

- `cd backend && python3 -m pytest ../tests/backend/test_public_mode_read_tightening.py -q` -> 12 passed, 1 warning.
- `PYTHONPATH=backend python3` check of `_PUBLIC_MODE_HIDDEN_KEYS - _DEFAULTS` -> `[]`.
