# Review: PR 340 Round 2

Date: 2026-08-14
Reviewed: `c807520..62c999b`
Round: 2
Label applied: `approved-by-codex-agent`

## What Is Correct

The round-1 public-droplet setup blocker is closed. In `frontend/src/components/setup/SetupWizard.tsx`, `handleFinish` derives `isPublicDroplet` from the same `state.driverType` that is sent as `station_driver_type`, calls `/api/setup/complete`, and only calls `setupAdmin(...)` when the driver is not `public_relay`. Since `/api/auth/setup-admin` is the POST blocked after `/api/setup/complete` flips public mode, the public-relay wizard path no longer makes that request after the mode change.

The step-3 account plumbing is consistent. `SetupWizard` passes `driverType` into `StepAccount`, and `StepAccount` renders the public-droplet placard for exactly `public_relay`; non-public drivers keep the existing account form. The step-3 advance predicate also now returns true for `public_relay`, so the skip is reflected in the wizard state.

The identity-push backoff fix is correct for the round-1 issue. `_maybe_push_identity` now checks `now - self._last_upload < self._effective_interval` before posting. `_last_upload` is advanced by successful reading pushes and by `_apply_backoff()` when the reading path enters or extends backoff, so a failed reading push that anchors backoff prevents the immediately following identity push from escaping the throttle window.

The new backend test covers the reported failure mode: it drives reading pushes into backoff with failed POSTs, then verifies the next broadcast makes no additional POST calls.

## Blockers

None.

## What Needs Attention

None.

## Bloat / Non-Functional

None.

## Recommendations

The round-2 fixes add several task-specific code comments. They are not blocking, but a later cleanup pass could shorten comments that mention review rounds or issue history; the behavior is now clear enough from the branch and tests.

## Verification

- `cd backend && python3 -m pytest ../tests/backend/test_public_relay_sender.py -q` — passed, 15 tests.
- `cd frontend && npx tsc --noEmit` — passed.

## Bottom Line

Approve. The blocker is closed, the identity backoff now shares the reading throttle correctly, and I found no regression in the `StepAccount` prop plumbing or public-droplet completion path in the `c807520..62c999b` diff.
