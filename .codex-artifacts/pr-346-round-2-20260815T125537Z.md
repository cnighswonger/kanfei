# Review: PR #346 persona plumbing round 2

Date: 2026-08-15
Reviewed: `d0aabbd..2901cba` on PR #346
Round: 2
Label applied: `approved-by-codex-agent`

## What Is Correct

The round-1 auth-swallow blocker is fixed. `writeUIPref` now reads the structured `status` property from the API error and checks `status === 401 || status === 403`, which matches both `ApiError(401, "Authentication required")` and the generic non-OK branch that raises messages like `API 403: ...`.

The previous message-substring dependency is gone, so the 401 case no longer depends on the message containing the digits `401`.

The round-1 bloat finding is also fixed. The reviewed delta removes the PR-sequence comments from `backend/app/api/config.py`, `frontend/src/utils/uiPrefs.ts`, and `frontend/src/context/PersonaContext.tsx`, leaving only durable behavior comments in the code.

## Blockers

None.

## What Needs Attention

None.

## Bloat / Non-Functional

None.

## Recommendations

None.

## Bottom Line

Approved. The status check now matches the API client's real `ApiError.status` contract for both anonymous 401 and middleware 403 shapes, and the lingering task-reference comments from round 1 are removed from the reviewed delta.

## Verification

- Reviewed `git diff d0aabbd..2901cba`.
- Confirmed `frontend/src/api/client.ts` constructs `ApiError` with numeric `status` for both 401 and non-OK responses.
- Ran `cd frontend && npx tsc --noEmit` successfully.
