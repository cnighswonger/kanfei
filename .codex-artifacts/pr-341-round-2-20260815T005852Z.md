# Review: PR #341 public-relay beta32 hotfixes

Date: 2026-08-15
Reviewed: round-2 delta `d115e0e`..`b84d1b8`
Round: 2
Label applied: approved-by-codex-agent

## What Is Correct

- `frontend/src/pages/MapView.tsx:263` derives `effectiveDefaultLayer` inside `BaseLayers`, after `showOsmRoads` is known and before every base-layer `checked` prop is computed.
- Public mode now maps the hidden `Roads` default to `Map`, so the local state default, `/api/config` value, and Settings-saved preference all pass through the same fallback before React-Leaflet mounts the base layers.
- `Map`, `Satellite`, and `Terrain` continue to compare against the effective value, and `Roads` is still rendered and checked normally for private stations because `showOsmRoads` remains true there.

## Blockers

None.

## What Needs Attention

None. A corrupt or out-of-contract `map_default_layer` value outside `Map` / `Roads` / `Satellite` / `Terrain` could still leave no base layer checked, but that is pre-existing and not a path that sets the value to `Roads`.

## Bloat / Non-Functional

None. The fallback is a small local normalization at the same boundary that decides whether the `Roads` layer is rendered.

## Recommendations

- No code changes required for this round.

## Verification

- `cd frontend && npx tsc --noEmit` passed.
- `cd frontend && npm run build` passed. Vite emitted the documented non-blocking chunk-size warning.

## Bottom Line

Approve. The round-1 blocker is fixed: public droplets with `defaultLayer === "Roads"` now mount the Carto-backed `Map` base layer, and private stations retain the original `Roads` default behavior.
