# Review: PR 339 public-relay ingest endpoints

Codex review: Round 2 fast follow-up review of the single new commit after prior approval.

Date: 2026-08-14
Reviewed: PR #339 diff from `29d059add0488d4fdb977d46c7aedc73282d3fc4` to `56f1d0ab601235a97c895ee3f7d94630bbd313f4`
Round: 2
Label applied: `approved-by-codex-agent`

## What Is Correct

The new test `tests/backend/test_ingest_endpoints.py::TestSecretMasking::test_secret_masked_in_public_mode_get_config` exercises the intended public-droplet read-bypass path. It sets `station_driver_type` to `public_relay`, stores `public_mode_ingest_secret`, invalidates the public-mode cache, and calls `GET /api/config` through `TestClient` without an `Authorization` header. That reaches the `require_admin` public-mode guest bypass rather than the normal authenticated path.

The assertion would catch the regression requested in round 1. `GET /api/config` masks values only when the config key is present in `_SECRET_KEYS`; if `public_mode_ingest_secret` were dropped from that set, this test would receive the stored raw value and fail on the explicit inequality assertion. The follow-up wildcard assertion also confirms the response is actually masked, not merely transformed to another unmasked string.

## Blockers

None.

## What Needs Attention

None.

## Bloat / Non-Functional

None. The follow-up is a focused regression test for the exact public-mode secret-leak path discussed in round 1.

## Recommendations

None.

## Bottom Line

Approved. The new test pins the unauthenticated public-mode config read path and would fail if `public_mode_ingest_secret` stopped being covered by `_SECRET_KEYS`.

## Verification

- `cd backend && python3 -m pytest ../tests/backend/test_ingest_endpoints.py -q` -> 13 passed, 1 warning.
